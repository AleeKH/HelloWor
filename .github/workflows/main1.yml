name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{2e09239a-09f7-4a58-8165-511f3aa8beac}}
          heroku_app_name: "testing2223" #Must be unique in Heroku
          heroku_email: "kienhui1@hotmail.com"
